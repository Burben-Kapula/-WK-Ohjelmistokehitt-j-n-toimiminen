# Secret key required in the special link to access admin login/panel.
# Generate a long random value, e.g.:
#   node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
VITE_ADMIN_SECRET_KEY=replace-with-a-long-random-secret
